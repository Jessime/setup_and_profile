git config --global user.name "Jessime Kirk"
git config --global user.email jessime.kirk@gmail.com

# Setup terminal and whatnot
sudo apt install tmux
echo "tmux is now installed at: $(which tmux)"
sudo apt install zsh
echo "zsh is now installed at: $(which zsh)"
chsh -s $(which zsh)

# Log out of something. It's either the terminal or ubuntu.

